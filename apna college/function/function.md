- funtion  is used to reduce rebundany
- it is a block of code which we can used anywhere in code by just having a function call 
- arguments are the real values which are passed through the function
- while parameaters are the copied value of the arguments!

